{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "bun",
      "request": "launch",
      "name": "Launch web-link-collector (Bun)",
      "program": "${workspaceFolder}/bin/web-link-collector.ts",
      "args": ["--configFile", "config.yaml", "--logLevel", "debug"],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "console": "integratedTerminal"
    }
  ]
}
